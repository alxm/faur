language: c

dist: trusty
sudo: required

before_install:
    - sudo apt update -qq
    - sudo apt --assume-yes -qq install libsdl2-dev libsdl2-mixer-dev

compiler:
    - clang
    - gcc

script:
    - ./bin/a2x_install
